var optimist = require('optimist');

describe('a test which pauses', function() {
  it('should pause', function() {
    browser.driver.get('http://localhost:8000/#/form');
    // var username = element(by.model('username'));

    // username.clear();

    console.log('argv:' + require('util').inspect(process.argv));

    expect(optimist.argv.run_local).toBeTruthy();

    // browser.pause();

    // username.sendKeys('Jane Doe');

    // var name = element(by.binding('username'));

    // expect(name.getText()).toEqual('Jane Doe');

    // console.log('history: ' + protractor.promise.controlFlow().getHistory());
    // console.log('---------');
    // console.log('schedule: ' + protractor.promise.controlFlow().getSchedule());
  });
})
